name: Documentation

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install zensical
        run: pip install zensical

      - name: Build docs
        run: zensical build --clean

      - name: Pack site
        run: tar -czf site.tar.gz -C site .

      - name: Upload to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          source: site.tar.gz
          target: /root/JsmacrosDoc/_incoming/

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          script: |
            set -e
            cd /root/JsmacrosDoc

            rm -rf site_new
            mkdir site_new
            tar -xzf _incoming/site.tar.gz -C site_new
            rm -f _incoming/site.tar.gz

            rm -rf site
            mv site_new site

            echo "Docs deployed successfully"